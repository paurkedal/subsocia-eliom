true: bin_annot, eliom_ppx

<web/server/*>: thread, \
    package(caqti), \
    package(caqti-lwt), \
    package(config-file), \
    package(eliom.server), \
    package(js_of_ocaml-ppx_deriving_json), \
    package(lwt), \
    package(lwt_log.core), \
    package(lwt_ppx), \
    package(panograph), \
    package(panograph.server), \
    package(prime), \
    package(prime.unstable), \
    package(react), \
    package(subsocia), \
    package(subsocia.data)

<web/client/*>: \
    package(eliom.client), \
    package(js_of_ocaml-ppx), \
    package(lwt), \
    package(lwt_log.core), \
    package(lwt_ppx), \
    package(panograph), \
    package(panograph.client), \
    package(prime), \
    package(prime.unstable), \
    package(react), \
    package(subsocia)
